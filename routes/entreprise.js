/*BEFORE LOGIN OR REGISTER*/

const express = require('express');
const router = express.Router();
const entreprise_ctrl = require('../controllers/entreprise_ctrl');


//router.get('/', entreprise_ctrl.);

module.exports = router;